package ru.itis.inf501.lab2_16.integral;

public class Integral {

    public static double[] totalSum;

    public Integral() {
        totalSum = new double[1];
    }

    public void calculate() {
/*
        // старт потоков
        Thread[] parts = new Thread[1];
        for (;;) {
            parts[1] = new Thread(new PartIntegralSum(...));
        }

        for () {
            parts[i].join();
        }

        sum ...
*/
    }

}
